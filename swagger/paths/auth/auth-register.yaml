tags:
  - Auth
summary: Register User API
description: Add a new User
operationId: addUser
requestBody:
  content:
    application/json:
      schema:
        $ref: ../../components/auth/AuthRegister.yaml
    application/xml:
      schema:
        $ref: ../../components/auth/AuthRegister.yaml
    application/x-www-form-urlencoded:
      schema:
        $ref: ../../components/auth/AuthRegister.yaml
responses:
  '201':
    description: Created